{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run PHP File",
            "type": "node-terminal",
            "request": "launch",
            "command": "clear && php ${file} && echo"
        }
    ]
}